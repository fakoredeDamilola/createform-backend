export enum EncryptionType {
  NONE = 'None',
  EMAIL = 'Email',
  CUSTOM = 'Custom',
}

export enum ResponseType {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
}

export enum FormItemType {
  QUESTION = 'QUESTION',
  STATIC = 'STATIC',
}

export enum FormStaticType {
  START = 'START',
  END = 'END',
}
